# Constants

Contiene constantes globales de la aplicación, como rutas (rutas de la app), valores por defecto de configuración y magic strings/numbers.

Ejemplos:
- `routes.ts`: `export const ROUTES = { home: '/', login: '/login' }`
- `config.ts`: Variables estáticas como `MAX_ITEMS_PER_LIST = 100`
