# Utils

Contiene funciones puras (utilidades matemáticas, formateadores de fecha, conversores) que no tienen efectos secundarios ni dependen del estado de React.

Ejemplo:
- `date-formatters.ts`: Para formatear las fechas con Intl o similar.
- `currency.ts`: Para mostrar montos monetarios de forma consistente.
